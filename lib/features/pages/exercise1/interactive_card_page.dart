import 'package:flutter/material.dart';

class InteractiveCardPage extends StatelessWidget {
  const InteractiveCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
